const DIRS = {
  source: "src",
  templates: "templates",
  components: "components",
  output: "dist",
};

const FILES = {
  mimeConfig: "./mime.config.json",
};

module.exports = {
  DIRS,
  FILES
};
